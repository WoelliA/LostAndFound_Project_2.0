LostAndFound.Model.ConfigModel = (function () {
    var that = {},

        init = function () {

            return that;
        },

             getItemTypes = function (callback) {

             };

    that.getItemTypes = getItemTypes;

    that.init = init;
    return that;

}());