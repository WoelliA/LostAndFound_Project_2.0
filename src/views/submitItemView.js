LostAndFound.Views.SubmitItemView = (function () {
    var that = {},

        init = function () {
            console.log("SubmitItem init");

            return that;
        };

    that.init = init;
    return that;
}());