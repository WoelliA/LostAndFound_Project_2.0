LostAndFound.Views.LoginView = (function () {
    var that = {},

        init = function () {
            console.log("LoginView init");

            return that;
        };


    that.init = init;
    return that;
}());